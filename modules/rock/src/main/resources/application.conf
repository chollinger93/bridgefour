{
  self = {
    id = 0
    id = ${?WORKER_ID}
    max-slots = 2
    schema = "http"
    host = localhost
    port = 5554
    port = ${?WORKER_PORT}
    probing-timeout = 0.5 seconds
  }
  leader = {
    schema = "http"
    host = localhost
    port = 5555
    port = ${?LEADER_PORT}
  }
}